import React from "react";
import spinner from "../images/2.svg";

import s from "./Spinner.module.css";

const Spinner = () => {
  return (
    <div>
      <img src={spinner} />
    </div>
  );
};

export default Spinner;
